23. Find the power set of each of these sets, where a and b are distinct elements.

a) {a}
b) {a, b}
c) {∅, {∅}}

---

a) {a}

The power set P(S) is the set of all subsets of S

P({a}) = {∅, {a}}

b) {a, b}

- Expected count: If a set has n elements, it's power set has $2^n$ elements.
  For S = {a,b}, n = 2 so there must be:

$2^2$ = 4 sebsets

- List subsets by size
  - Subset with 0 elements: ∅
  - Subset with 1 element: {a}, {b}
  - Subset with 2 elements: {a,b}

P({a,b}) = {∅, {a,b}, {a,b}}

c) {∅, {∅}}

- List all subsets by size
  - 0 elements: ∅
  - 1 element: {0}, {{∅}}
  - 2 elements: {∅, {∅}}

P(∅, {∅}, {{∅}}, {∅, {∅}})
