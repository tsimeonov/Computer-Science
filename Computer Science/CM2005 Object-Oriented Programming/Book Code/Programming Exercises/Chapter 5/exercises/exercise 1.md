1. Mark the following statements as true or false.

a. A loop is a control structure that causes certain statements to execute over and over.

True

b. In a counter-controlled while loop, it is not necessary to initialize the loop control variable.

False

c. It is possible that the body of a while loop may not execute at all.

True

d.In an infinite while loop, the while expression (the decision maker) is initially false, but after the first iteration it is always true.

False

e. The while loop:
j = 0;
while (j <= 10)
j++;
terminates if j > 10.

True

f. A sentinel-controlled while loop is an event-controlled while
loop whose termination depends on a special value.

True

g. To read data from a file of an unspecified length, an EOF-­ controlled
loop is a good choice.

True

h. When a while loop terminates, the control first goes back to the
statement just before the while statement, and then the control
goes to the statement immediately following the while loop.

False

i. A do...while loop is called a pretest loop

False

j. Executing a break statement in the body of a loop immediately terminates the loop.

True
